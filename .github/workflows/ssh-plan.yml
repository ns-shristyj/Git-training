name: SSH ACTION

on:
  push:
    branches: [ "ns-cquinlan" ]
  pull_request:
    branches: [ "ns-cquinlan" ]

jobs:
  deploy:
    runs-on: ["self-hosted", "linux", "x64", "sz-XS", "gh", "cheap"]
    steps:
      - uses: actions/checkout@v4
      - name: Install SSH Key
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: 54.183.103.225
          username: ubuntu
          key: ${{ secrets.SECENG_INTERN_KEY_PAIR }}
          port: 22
          command_timeout: 30m
          script: | 
            echo "Nice Job!!!"